package com.debugtools.core.window

enum class DisplayMode {
    /** Full panel visible on the right side of the screen. */
    EXPANDED,
    /** Small draggable floating button. */
    MINIMIZED,
    /** Compact strip (vertical on right, or horizontal at top/bottom). */
    BRIEF
}
