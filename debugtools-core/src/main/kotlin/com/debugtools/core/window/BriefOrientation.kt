package com.debugtools.core.window

enum class BriefOrientation {
    /** Vertical strip docked to the right edge (default). */
    VERTICAL,
    /** Horizontal bar at top or bottom of screen. */
    HORIZONTAL
}
